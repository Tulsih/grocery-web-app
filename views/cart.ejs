<%- include('partials/header') %>

<main class="main-content">
    <div class="container">
        <div class="page-header">
            <h2>Shopping Cart</h2>
        </div>

        <% if (cartItems.length === 0) { %>
            <div class="empty-cart">
                <div class="empty-cart-icon">🛒</div>
                <h3>Your cart is empty</h3>
                <p>Add some products to get started!</p>
                <a href="/products" class="btn btn-primary">Browse Products</a>
            </div>
        <% } else { %>
            <div class="cart-content">
                <div class="cart-items">
                    <% cartItems.forEach(item => { %>
                        <div class="cart-item" data-item-id="<%= item._id %>">
                            <img src="<%= item.product.image %>" alt="<%= item.product.name %>">
                            <div class="cart-item-details">
                                <h3><%= item.product.name %></h3>
                                <p class="item-price">₹<%= item.product.price.toFixed(2) %> / <%= item.product.unit %></p>
                            </div>
                            <div class="cart-item-actions">
                                <div class="quantity-controls">
                                    <button class="qty-btn decrease" data-item-id="<%= item._id %>">-</button>
                                    <span class="quantity"><%= item.quantity %></span>
                                    <button class="qty-btn increase" data-item-id="<%= item._id %>">+</button>
                                </div>
                                <p class="item-total">₹<%= (item.product.price * item.quantity).toFixed(2) %></p>
                                <button class="btn-remove" data-item-id="<%= item._id %>">🗑️</button>
                            </div>
                        </div>
                    <% }) %>
                </div>

                <div class="cart-summary">
                    <h3>Order Summary</h3>
                    <div class="summary-row">
                        <span>Total Items:</span>
                        <span id="totalItems"><%= totalItems %></span>
                    </div>
                    <div class="summary-row">
                        <span>Subtotal:</span>
                        <span id="subtotal">₹<%= totalPrice %></span>
                    </div>
                    <div class="summary-row">
                        <span>Delivery:</span>
                        <span>₹50.00</span>
                    </div>
                    <hr>
                    <div class="summary-row total">
                        <span>Total:</span>
                        <span id="totalPrice">₹<%= (parseFloat(totalPrice) + 50).toFixed(2) %></span>
                    </div>
                    <button class="btn btn-primary btn-block checkout-btn">Proceed to Checkout</button>
                </div>
            </div>
        <% } %>
    </div>
</main>

<%- include('partials/footer') %>